  <nav class="navbar navbar-expand-lg navbar-light bg-light container">
    <div class="container-fluid">
  
      <a href="/" class="navbar-brand">
      <svg xmlns="http://www.w3.org/2000/svg" width="1.2em" height="1.2em" fill="currentColor" class="bi bi-music-note-list d-inline-block align-text-top" viewBox="0 0 16 16">
        <path d="M12 13c0 1.105-1.12 2-2.5 2S7 14.105 7 13s1.12-2 2.5-2 2.5.895 2.5 2z"/>
        <path fill-rule="evenodd" d="M12 3v10h-1V3h1z"/>
        <path d="M11 2.82a1 1 0 0 1 .804-.98l3-.6A1 1 0 0 1 16 2.22V4l-5 1V2.82z"/>
        <path fill-rule="evenodd" d="M0 11.5a.5.5 0 0 1 .5-.5H4a.5.5 0 0 1 0 1H.5a.5.5 0 0 1-.5-.5zm0-4A.5.5 0 0 1 .5 7H8a.5.5 0 0 1 0 1H.5a.5.5 0 0 1-.5-.5zm0-4A.5.5 0 0 1 .5 3H8a.5.5 0 0 1 0 1H.5a.5.5 0 0 1-.5-.5z"/>
      </svg>
       Tune Geeks
      </a>

 

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <%= link_to "All Listings", listings_path, class:"nav-link"%>
        <%= link_to "Artists", artists_path, class:"nav-link"%>
      </div>

  
  
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <%# Only allow navigation items if user has created a username %>
  
            <% if user_signed_in? && Profile.where(user_id: current_user.id).exists? %>
                <li class="nav-item">
                    <%= link_to "Sell an item", new_listing_path, class:"nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Your account", profile_path(current_user.profile.id), class:"nav-link" %>
                </li>
            <% end %>
  
  
  
         <div class="nav-button">
           <% if user_signed_in? %>
              <li class="nav-item">
                  <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link" %>
              </li>
           <% else %>
                <li class="nav-item">
                    <%= button_to "Log in / Join", new_user_session_path, class: "nav-link " %>
                </li>
          <% end %>
         </div>
        </ul>
      </div>
    </div>
  </nav>